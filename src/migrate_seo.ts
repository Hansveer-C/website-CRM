import 'dotenv/config';

// Migration script for SEO fields. 
// Requires standard Postgres connection (not available via REST API).
console.log('--- Migration Manual Run Required ---');
console.log('Please see artifacts/seo_migration.md for SQL commands.');
