{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api(?:/.*)?", "dest": "/api/not-found" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
